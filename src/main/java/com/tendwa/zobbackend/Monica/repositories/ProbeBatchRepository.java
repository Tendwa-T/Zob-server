package com.tendwa.zobbackend.Monica.repositories;

import com.tendwa.zobbackend.Monica.entities.ProbeBatch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProbeBatchRepository extends JpaRepository<ProbeBatch, Integer> {
}